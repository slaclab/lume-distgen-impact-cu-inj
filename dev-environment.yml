name: lume-distgen-impact-cu-inj-dev
channels:
  - conda-forge
dependencies:
  - click
  - pydantic # until lume-services registered with conda
  - graphviz
  - python-graphviz
  - prefect[viz]<2.0
  - numpy
  - scikit-image
  - lume-impact
  - distgen
  - pandas
  - importlib-metadata
  - pip 
  - pip: 
    - git+https://github.com/slaclab/lume-model.git@main # until lume-services registered with conda
    - git+https://github.com/slaclab/lume-services.git@main

  # dev
  - pre-commit
  - pytest
  - pytest-cov
  - jupyterlab
